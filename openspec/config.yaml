schema: spec-driven

context: |
  Tech stack: Next.js 14+ (App Router), TypeScript, Tailwind CSS, shadcn/ui, Supabase, Vercel
  We use conventional commits
  Domain: Customer portal for NextMove AI - manages customers, contracts, time entries, priorities, requests
  Roles: admin, staff, customer_user
  See AGENTS.md for coding standards and project.md for full specification

rules:
  tasks:
    - Break tasks into chunks completable in one session
    - Group related tasks under numbered headings
    - Each task should be verifiable
